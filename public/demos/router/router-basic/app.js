/**
 * Vue Router 基础路由配置演示
 *
 * 核心知识点：
 * 1. 定义路由组件
 * 2. 配置 routes 数组
 * 3. 创建 VueRouter 实例
 * 4. 使用 router-link 和 router-view
 */

// ==================== 1. 定义路由组件 ====================
// 每个路由对应一个组件
// 实际项目中，这些通常是导入的 .vue 文件

const Home = {
  template: `
    <div>
      <h2>🏠 首页</h2>
      <p>欢迎来到 Vue Router 学习项目！</p>
      <p>点击上方导航切换不同页面。</p>
    </div>
  `
}

const About = {
  template: `
    <div>
      <h2>ℹ️ 关于我们</h2>
      <p>这是一个用于学习 Vue Router 的演示项目。</p>
      <p>Vue Router 是 Vue.js 官方的路由管理器。</p>
    </div>
  `
}

const Contact = {
  template: `
    <div>
      <h2>📞 联系方式</h2>
      <p>邮箱：example@vue.com</p>
      <p>电话：123-456-7890</p>
    </div>
  `
}

// ==================== 2. 配置路由规则 ====================
// routes 是一个数组，每个元素定义一条路由规则
// path: URL 路径
// component: 对应的组件

const routes = [
  {
    path: '/', // 根路径
    component: Home // 显示 Home 组件
  },
  {
    path: '/about', // /about 路径
    component: About // 显示 About 组件
  },
  {
    path: '/contact', // /contact 路径
    component: Contact // 显示 Contact 组件
  }
]

// ==================== 3. 创建路由实例 ====================
// 将路由配置传入 VueRouter

const router = new VueRouter({
  routes // ES6 简写，等同于 routes: routes
})

// ==================== 4. 创建 Vue 实例并挂载路由 ====================
// 通过 router 选项注入路由

new Vue({
  el: '#app',
  router // 注入路由，使整个应用都能使用路由功能
})

/**
 * 小结：
 *
 * 1. <router-link to="/path"> 用于导航，会渲染为 <a> 标签
 * 2. <router-view> 是路由出口，匹配的组件会渲染在这里
 * 3. 当前路由信息可通过 this.$route 访问
 * 4. 路由实例可通过 this.$router 访问
 */
